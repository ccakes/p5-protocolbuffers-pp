# CLAUDE.md

## Project Overview

ProtocolBuffers::PP is a pure-Perl Protocol Buffers implementation. It provides binary wire format encoding/decoding, canonical JSON (ProtoJSON) mapping, a `protoc` code generator plugin (`protoc-gen-perl`), and a gRPC client.

## Key Commands

```bash
# Run unit tests (225 tests)
prove -r t/unit/

# Run the official protobuf conformance suite (2737/2737 passing)
script/protobuf-conformance-test --protobuf-root /path/to/protobuf

# Run the gRPC conformance suite (211/211 passing)
script/grpc-conformance-test --connectrpc-root /path/to/connectrpc-conformance

# Generate Perl code from .proto files
protoc --perl_out=lib/ --plugin=protoc-gen-perl=script/protoc-gen-perl \
    -I /path/to/includes file.proto

# Install dependencies
cpanm --installdeps .
```

## Architecture

### Runtime (lib/ProtocolBuffers/PP/)

- **Encode.pm / Decode.pm** — Binary protobuf encoding/decoding. Descriptor-driven: iterate field descriptors by number, encode/decode based on type. Oneofs tracked via `_oneof_case` hash in messages.
- **Wire/** — Low-level wire format: Varint.pm (variable-length ints), ZigZag.pm (signed int encoding), Bytes.pm (fixed-width, length-delimited), Tags.pm (field tag encode/decode).
- **JSON/Print.pm** — Message-to-JSON. WKT dispatch table for special types. Uses `FloatLiteral` (Math::BigFloat subclass) for precise float formatting through JSON::PP.
- **JSON/Parse.pm** — JSON-to-message. WKT dispatch table. Uses `B::svref_2object` SV flags to distinguish JSON numbers from JSON strings (critical for Struct/Value).
- **Types.pm** — Constants for all protobuf field types (TYPE_DOUBLE, TYPE_STRING, etc.) and labels.
- **Util.pm** — `default_value_for_type`, `is_packable`, helper functions.
- **Error.pm** — Structured errors with category (decode, json, etc.) that stringify for eval/die pattern.
- **Bootstrap.pm** — Hand-built descriptors for `google.protobuf.compiler.CodeGeneratorRequest/Response` so the generator can decode its own input without generated code.
- **Generator.pm** — Generates Perl packages from parsed proto descriptors. Produces message classes with `__DESCRIPTOR__` methods, accessor methods with oneof semantics, and enum classes.
- **Timestamp.pm, Duration.pm, FieldMask.pm, Any.pm** — Well-Known Type string conversion helpers.
- **GRPC/Client.pm** — Pure-Perl gRPC client over HTTP/2. Supports unary, client-streaming, server-streaming, and bidirectional streaming RPCs.
- **GRPC/Transport.pm** — HTTP/2 transport layer for the gRPC client.
- **GRPC/Status.pm** — gRPC status code constants and helpers.

### Generated Code (lib/Google/Protobuf/)

Generated by `protoc-gen-perl`. Each message class has:
- `__DESCRIPTOR__` method returning a hash with field definitions, field lookups by name/json_name, oneofs, and resolved `message_descriptor` references.
- Accessor methods for each field (with oneof clearing logic for oneof members).
- Enum classes with `name_for`/`value_for` lookups.

**Note:** Conformance test types (Conformance::*, Connectrpc::*, test messages) are **not** checked into the repo. They are generated into a temp directory at test time by the wrapper scripts.

### Message Representation

Messages are plain Perl hashes (not blessed objects during encode/decode). Special keys:
- `_oneof_case` — `{ oneof_index => active_field_number }` tracks which oneof field is set
- `_unknown_fields` — Raw bytes of unrecognized fields, preserved for round-tripping

### Conformance Testing

Wrapper scripts under `script/` handle code generation into a temp directory and runner invocation:

- **script/protobuf-conformance-test** — Runs the official protobuf conformance suite. Accepts `--protobuf-root DIR` (or `$PROTOBUF_ROOT`). Generates types via `protoc`, finds `conformance_test_runner`, runs with `PROTOBUF_PP_GENDIR` set.
- **script/grpc-conformance-test** — Runs the ConnectRPC gRPC conformance suite. Accepts `--connectrpc-root DIR` (or `$CONNECTRPC_ROOT`). Generates types via `protoc`, finds `connectconformance`, runs with `PROTOBUF_PP_GENDIR` set.

Harness scripts under `script/`:

- **script/perl-conformance-harness** — Protobuf conformance harness. Pipe-based protocol: reads 4-byte LE length prefix + protobuf ConformanceRequest, writes ConformanceResponse. Loads generated types from `$PROTOBUF_PP_GENDIR`. Maintains a type registry for Any resolution.
- **script/perl-grpc-conformance-client** — gRPC conformance client. Reads 4-byte BE length prefix + ClientCompatRequest, makes gRPC calls to the reference server, writes ClientCompatResponse. Loads generated types from `$PROTOBUF_PP_GENDIR`.

Conformance config files live under `conformance/`:

- `conformance/protobuf-known-failing.txt` — Expected protobuf test failures (currently empty).
- `conformance/grpc-client-config.yaml` — gRPC client feature declaration.
- `conformance/grpc-client-known-failing.txt` — Expected gRPC test failures (currently empty).

## Important Patterns

- **Perl string semantics matter**: `utf8::decode()` on protobuf TYPE_STRING output marks bytes as character strings for JSON::PP; `utf8::encode()` converts back before wire encoding. JSON::PP with `utf8(1)` expects raw UTF-8 bytes, not character strings.
- **Perl falsy values**: Use `//` (defined-or) not `||` for protobuf payloads — the byte `\x30` (ASCII "0") is a valid payload but falsy in Perl. Same issue with empty-string field defaults.
- **JSON number vs string distinction**: JSON::PP sets `SVp_IOK`/`SVp_NOK` flags on numbers and `SVp_POK` on strings. The `_is_json_number()` function in Parse.pm uses `B::svref_2object` to inspect these flags. This is essential for google.protobuf.Value (Struct) to correctly map JSON `"5678"` to string_value vs `5678` to number_value.
- **Proto3 default omission**: Singular non-oneof fields at their default value are omitted from both binary encoding and JSON output.
- **Map fields**: Encoded as repeated MapEntry messages. Descriptors have `map_entry` with `key_type`, `value_type`, `value_message_descriptor`, `value_enum_class`.

## External Dependencies

- **protoc** — Only needed for code generation, not at runtime.
- **conformance_test_runner** — Built from protobuf source. The wrapper script locates it automatically.
- **connectconformance** — Built from connectrpc-conformance source. The wrapper script locates it automatically.
- **Perl modules** — JSON::PP, MIME::Base64, Math::BigInt, POSIX (all core or near-core). Test deps: Test::More, Test::Exception.

## Test Status

- **Unit tests**: 225/225 passing (`prove -r t/unit/`)
- **Protobuf conformance (required)**: 2737/2737 passing (0 unexpected failures)
- **Protobuf conformance (recommended)**: 33 warnings (non-blocking; includes unsupported features like extensions, unknown enum ignore-and-skip, FieldMask validation)
- **gRPC conformance**: 211/211 passing (0 failures)

## Documentation Guidelines

- **Feature docs**: Create a description document under `docs/` for each major feature (see `docs/protobuf-conformance.md` and `docs/grpc-conformance.md` for examples).
- **Keep root docs current**: When making changes that affect project usage, commands, architecture, or test status, update `CLAUDE.md` and `README.md` accordingly.
- **No raw file listings**: Do not put file tree dumps in `CLAUDE.md` or `README.md`. Use tables or prose to describe project layout — file trees are noise that goes stale quickly.
